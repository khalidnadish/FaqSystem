import { Typography } from "@mui/material";

const JoinigDate = ({joindate}) => {
  return (
    <Typography>
      {`FA :${joindate}`}
      "10101240"
    </Typography>
  );
};
export default JoinigDate;
